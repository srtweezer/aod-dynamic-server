#ifndef AOD_CONFIG_H
#define AOD_CONFIG_H

// =============================================================================
// AOD Dynamic Server - Configuration (Generated)
// =============================================================================
// This file is automatically generated from config.cmake by CMake
// DO NOT EDIT THIS FILE DIRECTLY - Edit config.cmake instead
// =============================================================================

#include <string>
#include <cstddef>

namespace aod {
namespace config {

// -----------------------------------------------------------------------------
// Server Configuration
// -----------------------------------------------------------------------------

constexpr int SERVER_PORT = @SERVER_PORT@;
constexpr const char* SERVER_BIND_ADDRESS = "@SERVER_BIND_ADDRESS@";

// -----------------------------------------------------------------------------
// AWG Hardware Configuration
// -----------------------------------------------------------------------------

constexpr int AWG_SERIAL_NUMBER = @AWG_SERIAL_NUMBER@;
constexpr int AWG_CHANNEL_MASK = @AWG_CHANNEL_MASK@;
constexpr int AWG_SAMPLE_RATE = @AWG_SAMPLE_RATE@;
constexpr double AWG_MAX_AMPLITUDE = @AWG_MAX_AMPLITUDE@;

constexpr bool AWG_EXTERNAL_CLOCK_ENABLED = @AWG_EXTERNAL_CLOCK_ENABLED_CPP@;
constexpr int AWG_EXTERNAL_CLOCK_FREQ = @AWG_EXTERNAL_CLOCK_FREQ@;

// -----------------------------------------------------------------------------
// Waveform Generation Parameters
// -----------------------------------------------------------------------------

constexpr int AOD_MAX_TONES = @AOD_MAX_TONES@;
constexpr int MAX_WAVEFORM_BATCHES = @MAX_WAVEFORM_BATCHES@;
constexpr int WAVEFORM_TIMESTEP = @WAVEFORM_TIMESTEP@;

constexpr size_t AOD_DMA_BUFFER_SIZE = @AOD_DMA_BUFFER_SIZE@;
constexpr size_t AOD_NOTIFY_SIZE = @AOD_NOTIFY_SIZE@;
constexpr size_t AOD_HW_BUFFER_SIZE = @AOD_HW_BUFFER_SIZE@;

// -----------------------------------------------------------------------------
// GPU/CUDA Configuration
// -----------------------------------------------------------------------------

constexpr int GPU_DEVICE_ID = @GPU_DEVICE_ID@;
constexpr bool GPU_ENABLED = @GPU_ENABLED_CPP@;

constexpr int GPU_BLOCK_SIZE = @GPU_BLOCK_SIZE@;
constexpr int GPU_GRID_SIZE = @GPU_GRID_SIZE@;

// -----------------------------------------------------------------------------
// Logging Configuration
// -----------------------------------------------------------------------------

constexpr const char* LOG_LEVEL = "@LOG_LEVEL@";
constexpr const char* LOG_FILE = "@LOG_FILE@";

} // namespace config
} // namespace aod

#endif // AOD_CONFIG_H
